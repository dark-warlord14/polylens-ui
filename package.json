{
  "name": "polylens-ui",
  "version": "1.0.0",
  "description": "PolyLens UI converted from Chrome Extension",
  "main": "index.js",
  "scripts": {
    "sync": "node scripts/sync.js",
    "test": "for f in tests/*.test.js; do echo \"-- $f\"; node \"$f\" || exit 1; done",
    "serve": "npx http-server src -p 8080 -c-1",
    "dev": "npm run sync && npm run serve",
    "start": "npm run serve"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
